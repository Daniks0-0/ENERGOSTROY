import React from "react";

const SamplePartners = (props) => {
    return (
        <div className="img-base">
               <img src = {props.img} alt="" />
        </div>
     
    );
};

export default SamplePartners;
